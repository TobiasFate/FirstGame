#ifndef PAUSE_H_INCLUDED
#define PAUSE_H_INCLUDED

#include <bits/stdc++.h>

#include <SDL.h>
#include <SDL_image.h>
#include <SDL_ttf.h>
#include <SDL_mixer.h>

#include "Box.h"
#include "ScreenSize.h"
#include "loadTexture.h"

void pause (int &state, int &sound, int &lives, int &score, SDL_Renderer* gRenderer) {

}

#endif // PAUSE_H_INCLUDED
