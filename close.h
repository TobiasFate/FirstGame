#ifndef CLOSE_H_INCLUDED
#define CLOSE_H_INCLUDED

#include <bits/stdc++.h>
#include <SDL.h>
#include <SDL_image.h>
#include <SDL_ttf.h>
#include <SDL_mixer.h>

void close(SDL_Renderer* &renderer, SDL_Window* &window) ;

#endif // CLOSE_H_INCLUDED
